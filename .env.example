# Database
MONGODB_URI=mongodb://localhost:27017/english_learning_app

# JWT Secrets (QUAN TRỌNG: Đổi trong production!)
JWT_SECRET=your-super-secret-access-token-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-different-from-access

# Token Expiration
JWT_EXPIRES_IN=15m          # Access token: 15 phút
JWT_REFRESH_EXPIRES_IN=7d   # Refresh token: 7 ngày

# Cookie Settings
COOKIE_MAX_AGE=2592000000   # 30 ngày (milliseconds)
COOKIE_SECURE=false         # false cho development, true cho production
COOKIE_DOMAIN=              # Optional: .yourdomain.com cho subdomain sharing

# Environment
NODE_ENV=development        # development hoặc production

# CORS Settings
CLIENT_URL=http://localhost:5173  # Frontend URL, có thể nhiều domain cách nhau bằng dấu phẩy
                                   # Production: https://admin.yourdomain.com,https://app.yourdomain.com

# Server
PORT=3000

# ============================================
# PRODUCTION CONFIGURATION EXAMPLE
# ============================================
# Khi deploy lên production, sửa các giá trị sau:
#
# NODE_ENV=production
# COOKIE_SECURE=true
# CLIENT_URL=https://admin.yourdomain.com,https://app.yourdomain.com
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
# JWT_SECRET=<random-strong-secret-key>
# JWT_REFRESH_SECRET=<another-random-strong-secret-key>
# COOKIE_DOMAIN=.yourdomain.com  # Nếu frontend và backend ở subdomain khác nhau
